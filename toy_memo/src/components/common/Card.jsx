import styled from "styled-components";

const Scard = styled.div`
    width: 100%;
    height: calc(100%/3);
    border: 1px solid black;
`;

function Card(props) {
    return (
        <Scard>
            
        </Scard>
    );
}

export default Card;